<div class="page-header">
  <h1>Tous les statuts</h1>
</div>

<%= link_to "Ajouter un statut", new_status_path, class:"btn btn-default" %>

<% @statuses.each do |status| %>
<div class="status">
    <strong>Nom</strong>
    <p><%= status.status %></p>
  <div class="meta">
     <%= link_to time_ago_in_words(status.created_at) + " ago", status %>
    <span class="admin">
     | <%= link_to "Editer", edit_status_path(status) %> |
      <%= link_to "Supprimer", method: :delete, data: {confirm: "Êtes-vous sûr de vouloir supprimer ce statut ?"} %>
    </span>
  </div>
</div>
  <% end %>